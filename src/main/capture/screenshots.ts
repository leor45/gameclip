/**
 * Captura de pantalla del monitor de grabación → PNG en <outputDir>/Capturas.
 * Implementación real en la tarea main (agente A); el stub mantiene el contrato.
 */
export async function takeScreenshot(
  monitorIndex: number,
  outputDir: string,
): Promise<string | null> {
  void monitorIndex;
  void outputDir;
  return null;
}
