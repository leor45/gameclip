# Preferencias por máquina (nunca al repo)
preferences.local.json

# Dependencias y builds
node_modules/
dist/
out/
release/
*.tsbuildinfo

# Helper nativo compilado: se genera desde native/ con scripts/build-haptic-mute.ps1
# (se compila en build:portable). El fuente y el script sí van al repo; el binario no.
resources/gc-app-audio-mute.exe

# Datos locales
*.sqlite
*.sqlite3
*.db
server/data/

# Entorno
.env
.env.*
!.env.example

# Logs y temporales (incluye build/icon.log, la traza de `npm run icon`)
*.log
.DS_Store
Thumbs.db
